<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">

            <!-- <xpath expr="//sheet" position="before">
                <header>
                    
                    
                    <button name="action_draft" type="object" string="Set to draft" class="btn-secondary" invisible="state == 'draft' or not is_vendor"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,waiting_approval,approved" invisible="not is_vendor"/>
                </header>
                
            </xpath> -->
            
            <!-- <xpath expr="//field[@name='vat']" position="attributes">
                <attribute name="required">is_customer</attribute>
            </xpath> -->

            <xpath expr="//field[@name='category_id']" position="after">
                <field name="is_customer" />
                <field name="is_vendor" />
                <field name="supplier_site" invisible="not is_vendor" required="is_vendor"/>
            </xpath>

            <!-- <xpath expr="//notebook" position="inside">
                <page string="Tier Approval" invisible="not is_vendor">
                    <field name="approval_history_ids" />
                </page>
            </xpath> -->

        </field>
    </record>

    
    <record id="account.res_partner_action_supplier" model="ir.actions.act_window">
        <field name="name">Vendors</field>
        <field name="res_model">res.partner</field>
        <field name="path">vendors</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_supplier': 1,'res_partner_search_mode': 'supplier', 'default_is_company': True, 'default_supplier_rank': 1, 'default_is_vendor': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new supplier in your address book
            </p>
            <p>
                Odoo helps you easily track all activities related to a supplier.
            </p>
        </field>
    </record>

    <record id="account.res_partner_action_customer" model="ir.actions.act_window">
        <field name="name">Customers</field>
        <field name="res_model">res.partner</field>
        <field name="path">customers</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_customer': 1,'res_partner_search_mode': 'customer', 'default_is_company': True, 'default_customer_rank': 1, 'default_is_customer': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new customer in your address book
            </p>
            <p>
                Odoo helps you easily track all activities related to a customer.
            </p>
        </field>
    </record>
    

</odoo>
