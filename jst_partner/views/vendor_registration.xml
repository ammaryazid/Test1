<?xml version="1.0" encoding="utf-8"?>
<odoo>

    
    <record id="vendor_registration_action" model="ir.actions.act_window">
        <field name="name">Vendor Registration</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="domain">[('is_vendor', '=', True),('active', '=', False)]</field>
        <field name="context">{"default_is_vendor": True, "default_active": False}</field>
    </record>

</odoo>
